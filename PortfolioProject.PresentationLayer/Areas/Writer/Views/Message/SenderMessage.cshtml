@model List<PortfolioProject.EntityLayer.Concrete.WriterMessage>
@{
    ViewData["Title"] = "Giden Mesajlar";
    ViewData["Subtitle"] = "Gönderdiğiniz tüm mesajları görüntüleyin";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<div class="row g-4">
    <!-- Header Section -->
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h2 class="text-primary mb-1">
                    <i class="fas fa-paper-plane me-2"></i>
                    Giden Mesajlar
                </h2>
                <p class="text-muted mb-0">Toplam @Model.Count mesaj gönderildi</p>
            </div>
            <div class="d-flex gap-2">
                <a href="/Writer/Message/CreateMessage" class="btn btn-primary">
                    <i class="fas fa-edit me-2"></i>
                    Yeni Mesaj
                </a>
                <a href="/Writer/Message/ReceiverMessage" class="btn btn-outline-secondary">
                    <i class="fas fa-inbox me-2"></i>
                    Gelen Mesajlar
                </a>
            </div>
        </div>
    </div>

    <!-- Messages List -->
    @if (Model.Any())
    {
        <div class="col-12">
            <div class="row g-3">
                @foreach (var item in Model)
                {
                    <div class="col-lg-6 col-xl-4">
                        <div class="message-card sent" data-bs-toggle="modal" data-bs-target="#messageModal"
                             data-receiver="@item.Receiver"
                             data-receivername="@item.ReceiverName"
                             data-subject="@item.Subject"
                             data-content="@item.Content"
                             data-date="@item.Date.ToString("dd MMMM yyyy")">
                            <div class="message-header">
                                <div class="receiver-info">
                                    <div class="receiver-avatar">
                                        <i class="fas fa-user"></i>
                                    </div>
                                    <div class="receiver-details">
                                        <h6 class="receiver-name mb-1">@item.ReceiverName</h6>
                                        <small class="text-muted">@item.Receiver</small>
                                    </div>
                                </div>
                                <div class="message-date">
                                    <span class="badge bg-success text-white">
                                        <i class="fas fa-check me-1"></i>
                                        Gönderildi
                                    </span>
                                </div>
                            </div>
                            <div class="message-content">
                                <h6 class="message-subject mb-2">@item.Subject</h6>
                                <p class="message-preview text-muted">
                                    @(item.Content.Length > 100 ? item.Content.Substring(0, 100) + "..." : item.Content)
                                </p>
                            </div>
                            <div class="message-footer">
                                <div class="message-meta">
                                    <small class="text-muted">
                                        <i class="fas fa-clock me-1"></i>
                                        @item.Date.ToString("dd MMM yyyy, HH:mm")
                                    </small>
                                </div>
                                <button class="btn btn-sm btn-outline-info w-100">
                                    <i class="fas fa-eye me-2"></i>
                                    Mesajı Gör
                                </button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <div class="col-12">
            <div class="empty-state text-center py-5">
                <div class="empty-icon mb-4">
                    <i class="fas fa-paper-plane fa-4x text-muted"></i>
                </div>
                <h4 class="text-muted mb-3">Henüz mesaj gönderilmedi</h4>
                <p class="text-muted mb-4">Giden kutunuzda henüz mesaj bulunmuyor.</p>
                <a href="/Writer/Message/CreateMessage" class="btn btn-primary">
                    <i class="fas fa-edit me-2"></i>
                    İlk Mesajınızı Yazın
                </a>
            </div>
        </div>
    }
</div>

<!-- Message Detail Modal -->
<div class="modal fade" id="messageModal" tabindex="-1" aria-labelledby="messageModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="messageModalLabel">
                    <i class="fas fa-paper-plane me-2"></i>
                    Gönderilen Mesaj Detayları
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Kapat"></button>
            </div>
            <div class="modal-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold text-muted">Alıcı Adı</label>
                        <div class="form-control-plaintext" id="modalReceiverName"></div>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold text-muted">Alıcı Email</label>
                        <div class="form-control-plaintext" id="modalReceiver"></div>
                    </div>
                    <div class="col-12">
                        <label class="form-label fw-bold text-muted">Konu</label>
                        <div class="form-control-plaintext" id="modalSubject"></div>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold text-muted">Gönderim Tarihi</label>
                        <div class="form-control-plaintext" id="modalDate"></div>
                    </div>
                    <div class="col-12">
                        <label class="form-label fw-bold text-muted">Mesaj İçeriği</label>
                        <div class="message-content-display" id="modalContent"></div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Kapat</button>
                <a href="/Writer/Message/CreateMessage" class="btn btn-primary">
                    <i class="fas fa-edit me-2"></i>
                    Yeni Mesaj
                </a>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const messageCards = document.querySelectorAll(".message-card");
        const modalReceiver = document.getElementById("modalReceiver");
        const modalReceiverName = document.getElementById("modalReceiverName");
        const modalSubject = document.getElementById("modalSubject");
        const modalContent = document.getElementById("modalContent");
        const modalDate = document.getElementById("modalDate");

        messageCards.forEach(card => {
            card.addEventListener("click", function () {
                modalReceiver.textContent = this.dataset.receiver;
                modalReceiverName.textContent = this.dataset.receivername;
                modalSubject.textContent = this.dataset.subject;
                modalContent.textContent = this.dataset.content;
                modalDate.textContent = this.dataset.date;
            });
        });
    });
</script>

<style>
    .message-card.sent {
        background: white;
        border-radius: 20px;
        padding: 1.5rem;
        border: 2px solid var(--border-color);
        box-shadow: var(--shadow-md);
        transition: all 0.3s ease;
        cursor: pointer;
        height: 100%;
        display: flex;
        flex-direction: column;
        position: relative;
    }
    
    .message-card.sent::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(135deg, var(--success-color), var(--primary-color));
        border-radius: 20px 20px 0 0;
    }
    
    .message-card.sent:hover {
        transform: translateY(-5px);
        box-shadow: var(--shadow-lg);
        border-color: var(--success-color);
    }
    
    .message-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1rem;
    }
    
    .receiver-info {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .receiver-avatar {
        width: 50px;
        height: 50px;
        background: linear-gradient(135deg, var(--success-color), var(--primary-color));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.2rem;
    }
    
    .receiver-name {
        color: var(--text-primary);
        font-weight: 600;
        margin: 0;
    }
    
    .message-date .badge {
        font-size: 0.8rem;
        padding: 0.5rem 0.75rem;
        border-radius: 20px;
    }
    
    .message-content {
        flex: 1;
        margin-bottom: 1rem;
    }
    
    .message-subject {
        color: var(--text-primary);
        font-weight: 600;
        margin: 0;
    }
    
    .message-preview {
        font-size: 0.9rem;
        line-height: 1.5;
        margin: 0;
    }
    
    .message-footer {
        margin-top: auto;
    }
    
    .message-meta {
        text-align: center;
        margin-bottom: 0.75rem;
    }
    
    .empty-state {
        background: white;
        border-radius: 20px;
        padding: 3rem;
        box-shadow: var(--shadow-md);
    }
    
    .empty-icon {
        color: var(--text-secondary);
    }
    
    .message-content-display {
        background: var(--light-color);
        border: 2px solid var(--border-color);
        border-radius: 15px;
        padding: 1rem;
        min-height: 120px;
        line-height: 1.6;
        color: var(--text-primary);
    }
    
    .modal-content {
        border: none;
        border-radius: 20px;
        box-shadow: var(--shadow-lg);
    }
    
    .modal-header {
        background: linear-gradient(135deg, var(--success-color), var(--primary-color));
        color: white;
        border-radius: 20px 20px 0 0;
        border: none;
    }
    
    .modal-title {
        font-weight: 600;
    }
    
    .btn-close {
        filter: invert(1);
    }
    
    .form-control-plaintext {
        background: var(--light-color);
        border: 2px solid var(--border-color);
        border-radius: 10px;
        padding: 0.75rem 1rem;
        color: var(--text-primary);
        font-weight: 500;
    }
    
    @@media (max-width: 768px) {
        .message-card.sent {
            margin-bottom: 1rem;
        }
        
        .d-flex.justify-content-between {
            flex-direction: column;
            gap: 1rem;
            align-items: stretch;
        }
    }
</style>


